# Backend AI Assistant - dependencias Python
#
# ══════════════════════════════════════════════════════════════════════════════
# RECOMENDADO: instale apenas as deps dos providers que você vai usar.
#
#   python setup_env.py           ← lê config.json e instala só o necessário
#   python setup_env.py --check   ← mostra o que seria instalado
#   python setup_env.py --tts f5-tts --llm ollama --stt whisper
#
# Os arquivos por provider estão em: requirements/
# ══════════════════════════════════════════════════════════════════════════════
#
# Este arquivo instala TUDO (todos os providers).
# Use apenas para referenciar o conjunto completo ou CI/CD.
#
# CUDA 12.1+: certifique-se de ter CUDA Toolkit e cuDNN instalados.
# Alternativa via pip (se nao tiver cuDNN no sistema):
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12

-r requirements/base.txt
-r requirements/stt_whisper.txt
-r requirements/llm_anthropic.txt
-r requirements/llm_ollama.txt
-r requirements/llm_turboquant.txt
-r requirements/tts_edge.txt
-r requirements/tts_f5.txt
# tts_chatterbox requer passos manuais — veja requirements/tts_chatterbox.txt
# -r requirements/tts_chatterbox.txt
